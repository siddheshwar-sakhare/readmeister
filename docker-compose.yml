version: '3'

services:
  frontend:
    image: readmeister-frontend
    ports:
      - "3000:80"
    depends_on:
      - backend

  backend:
    image: readmeister-backend
    ports:
      - "1001:1001"
    environment:
      - frontend.url=http://localhost:3000